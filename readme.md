##### create
```text
// 基本用法
vmc create myProject

// 选择构建工具： webpack(默认) or gulp
-t, --tool [webpack|gulp]

// 是否自动安装依赖，默认 false
-i, --install

// 如果自动安装依赖，选择包管理工具，npm(默认) or yarn
-pt, --pkg-tool [npm|yarn]
```
